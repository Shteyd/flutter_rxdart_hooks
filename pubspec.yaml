name: flutter_rxdart_hooks
description: This package is intended to make it easier to use rxdart with flutter_hooks. Created hooks for working with BehaviorSubject
version: 0.0.1
homepage: https://github.com/Shteyd/flutter_rxdart_hooks

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.18.4
  rxdart: ^0.27.4

dev_dependencies:
  flutter_test:
    sdk: flutter
